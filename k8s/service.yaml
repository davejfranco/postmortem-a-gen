apiVersion: v1
kind: Service
metadata:
  name: postmortems-reports-service
  namespace: default
  labels:
    app: postmortems-reports
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: postmortems-reports
